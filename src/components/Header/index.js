import './styles.scss';

// == Composant
const Header = () => (
  <div className="header">
    <h1 className="header-title">Qui suis-je ?</h1>
    <p className="header-subtitle">Clique sur l'animal pour découvrir son nom et le son qu'il émet ! </p>
  </div>
);

// == Export
export default Header;
